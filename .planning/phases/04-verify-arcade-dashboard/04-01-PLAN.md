---
phase: 04-verify-arcade-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.sh
  - dashboard/stop.sh
autonomous: true

must_haves:
  truths:
    - "launch.sh starts HTTP server and refresh loop using the correct project directory"
    - "http://localhost:8787/index.html renders with NES.css pixel fonts and retro containers"
    - "System Vitals panel shows CPU, Memory, Disk percentages from live stats.json"
    - "News panels (Local, Breaking, Sports, Outdoors) show items from news.json"
    - "stop.sh cleanly kills both server and refresh loop processes"
  artifacts:
    - path: "config.sh"
      provides: "Correct MONITOR_HOME path pointing to ~/Documents/radio-free-albany"
      contains: "Documents/radio-free-albany"
    - path: "dashboard/stop.sh"
      provides: "Dynamic path resolution instead of hardcoded ~/system-monitor path"
      contains: "SCRIPT_DIR"
    - path: "dashboard/news-cache/stats.json"
      provides: "Live system stats written by system-stats.sh to correct location"
  key_links:
    - from: "config.sh"
      to: "dashboard/launch.sh"
      via: "source $SCRIPT_DIR/../config.sh sets DASHBOARD_DIR"
      pattern: "MONITOR_HOME.*Documents/radio-free-albany"
    - from: "dashboard/launch.sh"
      to: "dashboard/news-cache/stats.json"
      via: "system-stats.sh writes stats.json to NEWS_CACHE_DIR from config.sh"
      pattern: "NEWS_CACHE_DIR"
    - from: "dashboard/stop.sh"
      to: "dashboard/.refresh.pid"
      via: "reads PID file from dynamically resolved DASHBOARD_DIR"
      pattern: "SCRIPT_DIR"
---

<objective>
Fix the two critical path bugs (config.sh MONITOR_HOME, stop.sh hardcoded path) and verify the full Radio Free Albany arcade dashboard launches, renders all panels, and shuts down cleanly at localhost:8787.

Purpose: config.sh currently points MONITOR_HOME to ~/system-monitor (the old project location), causing launch.sh to serve the wrong index.html, run the old news-fetcher, and write stats.json to the wrong directory. stop.sh has the same path hardcoded. These two fixes are all that stand between the current codebase and a working dashboard.

Output: A working dashboard that serves the NES.css arcade theme with live system stats and news data.
</objective>

<execution_context>
@/home/tripp/.claude/get-shit-done/workflows/execute-plan.md
@/home/tripp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verify-arcade-dashboard/04-RESEARCH.md
@config.sh
@dashboard/stop.sh
@dashboard/launch.sh
@dashboard/system-stats.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix config.sh MONITOR_HOME and stop.sh hardcoded path</name>
  <files>config.sh, dashboard/stop.sh</files>
  <action>
  **config.sh (line 12):** Change the MONITOR_HOME default from `$HOME/system-monitor` to `$HOME/Documents/radio-free-albany`. The line currently reads:
  ```
  export MONITOR_HOME="${MONITOR_HOME:-$HOME/system-monitor}"
  ```
  Change it to:
  ```
  export MONITOR_HOME="${MONITOR_HOME:-$HOME/Documents/radio-free-albany}"
  ```
  This single change fixes the entire path chain: DASHBOARD_DIR, NEWS_CACHE_DIR, LOG_DIR, and ALERT_LOG all derive from MONITOR_HOME.

  **dashboard/stop.sh (lines 13-15):** Replace the hardcoded DASHBOARD_DIR with dynamic path resolution using the SCRIPT_DIR pattern (same pattern used by launch.sh and system-stats.sh). Replace:
  ```
  DASHBOARD_DIR="$HOME/system-monitor/dashboard"
  PID_FILE="$DASHBOARD_DIR/.refresh.pid"
  SERVER_PID_FILE="$DASHBOARD_DIR/.server.pid"
  ```
  With:
  ```
  SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  CONFIG_FILE="$SCRIPT_DIR/../config.sh"
  if [ -f "$CONFIG_FILE" ]; then
      source "$CONFIG_FILE"
  fi
  DASHBOARD_DIR="${DASHBOARD_DIR:-$SCRIPT_DIR}"
  PID_FILE="$DASHBOARD_DIR/.refresh.pid"
  SERVER_PID_FILE="$DASHBOARD_DIR/.server.pid"
  ```
  This sources config.sh (which now has the correct MONITOR_HOME), getting DASHBOARD_DIR from the config chain. Falls back to SCRIPT_DIR if config.sh is missing.

  Also update the USAGE comment at the top (line 10) from `~/system-monitor/dashboard/stop.sh` to `~/Documents/radio-free-albany/dashboard/stop.sh`.

  Do NOT modify launch.sh, system-stats.sh, news-fetcher.sh, or index.html -- they are correct as-is once config.sh is fixed.
  </action>
  <verify>
  Run these checks:
  1. `source /home/tripp/Documents/radio-free-albany/config.sh && echo $MONITOR_HOME` -- should print `/home/tripp/Documents/radio-free-albany`
  2. `source /home/tripp/Documents/radio-free-albany/config.sh && echo $DASHBOARD_DIR` -- should print `/home/tripp/Documents/radio-free-albany/dashboard`
  3. `source /home/tripp/Documents/radio-free-albany/config.sh && echo $NEWS_CACHE_DIR` -- should print `/home/tripp/Documents/radio-free-albany/dashboard/news-cache`
  4. `grep -c "system-monitor" /home/tripp/Documents/radio-free-albany/config.sh` -- should return 0 (no old path references)
  5. `grep -c "SCRIPT_DIR" /home/tripp/Documents/radio-free-albany/dashboard/stop.sh` -- should return 1+ (dynamic resolution present)
  6. `grep -c "system-monitor" /home/tripp/Documents/radio-free-albany/dashboard/stop.sh` -- should return 0 (no hardcoded old path)
  </verify>
  <done>
  config.sh MONITOR_HOME defaults to ~/Documents/radio-free-albany. stop.sh uses SCRIPT_DIR + config.sh for path resolution with no hardcoded ~/system-monitor references. All derived paths (DASHBOARD_DIR, NEWS_CACHE_DIR, LOG_DIR) resolve to the correct project location.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end dashboard launch, verify, and clean shutdown</name>
  <files>dashboard/news-cache/stats.json</files>
  <action>
  Run the full launch-verify-stop cycle to confirm the dashboard works end-to-end. This task creates no new files -- it exercises the existing scripts and verifies output.

  **Step 1: Kill any existing dashboard processes.** Run `fuser -k 8787/tcp 2>/dev/null` to free the port. Also kill any old refresh loops: check for stale PID files in both `~/system-monitor/dashboard/` and `~/Documents/radio-free-albany/dashboard/` and kill those PIDs if the files exist.

  **Step 2: Ensure news-cache directory exists.** Run `mkdir -p /home/tripp/Documents/radio-free-albany/dashboard/news-cache`.

  **Step 3: Launch the dashboard** with browser auto-open disabled (we verify via curl, not a browser):
  ```
  ENABLE_BROWSER_OPEN=0 /home/tripp/Documents/radio-free-albany/dashboard/launch.sh
  ```
  Wait 2 seconds for the server to start and initial data fetch to complete.

  **Step 4: Verify HTTP server responds.** Run `curl -s -o /dev/null -w "%{http_code}" http://localhost:8787/index.html` -- must return 200.

  **Step 5: Verify NES.css theme is in the served HTML.** Run `curl -s http://localhost:8787/index.html | grep -c "nes.css"` -- must return 1+ (confirms the arcade theme HTML is being served, not the old Drudge-style dashboard).

  **Step 6: Verify stats.json exists and has live data.** Run `curl -s http://localhost:8787/news-cache/stats.json` and confirm it contains `"timestamp"`, `"cpu"`, `"memory"`, `"disk"` keys. The timestamp should be within the last 60 seconds.

  **Step 7: Verify news.json is accessible.** Run `curl -s http://localhost:8787/news-cache/news.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d),'items')"` -- should print a positive number of items.

  **Step 8: Run stop.sh and verify clean shutdown.**
  ```
  /home/tripp/Documents/radio-free-albany/dashboard/stop.sh
  ```
  Then verify: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8787/index.html 2>/dev/null || echo "Connection refused (expected)"` -- should fail with connection refused, confirming the server is down. Verify no PID files remain: `ls /home/tripp/Documents/radio-free-albany/dashboard/.refresh.pid /home/tripp/Documents/radio-free-albany/dashboard/.server.pid 2>&1` should show "No such file".

  **If any step fails:** Diagnose the issue by checking the script output, the PID files, and the news-cache directory contents. Fix only the path-related issues identified in Task 1 -- do not refactor other parts of the scripts.
  </action>
  <verify>
  All of these must be true after running the launch-verify-stop cycle:
  1. `curl http://localhost:8787/index.html` returned HTTP 200 during the test
  2. The served index.html contained "nes.css" (arcade theme, not old Drudge layout)
  3. stats.json at localhost:8787/news-cache/stats.json had live CPU/memory/disk data
  4. news.json at localhost:8787/news-cache/news.json had a positive number of news items
  5. After stop.sh, port 8787 is free and PID files are removed
  </verify>
  <done>
  The full launch-verify-stop cycle completes successfully: dashboard serves the NES.css arcade index.html at localhost:8787, stats.json and news.json are both accessible with live data, and stop.sh cleanly shuts down all processes with no orphans or stale PID files.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. config.sh MONITOR_HOME points to ~/Documents/radio-free-albany (not ~/system-monitor)
2. stop.sh uses dynamic SCRIPT_DIR path resolution (no hardcoded paths)
3. launch.sh successfully starts HTTP server serving the arcade index.html
4. System stats and news data are accessible at localhost:8787/news-cache/
5. stop.sh cleanly kills all processes started by launch.sh
</verification>

<success_criteria>
- DASH-01: NES.css arcade theme confirmed in served index.html (grep for "nes.css" in HTTP response)
- DASH-02: launch.sh starts without errors, stop.sh shuts down cleanly (port freed, PIDs removed)
- DASH-03: All panels have data sources accessible -- stats.json for System Vitals, news.json for news panels, FaceBot at :4000 or graceful offline for BotSpace
</success_criteria>

<output>
After completion, create `.planning/phases/04-verify-arcade-dashboard/04-01-SUMMARY.md`
</output>
