# Logs and generated data
logs/*.log
logs/*.json
dashboard/news-cache/*.json

# PID files and runtime state
dashboard/.*.pid
logs/.bridge.pid
logs/.pending-break
logs/.botspace-lastseen

# Stray Windows redirect artifact (`> nul` instead of `> $null`)
nul

# Python
__pycache__/
*.py[cod]

# Editor
*.swp
*.swo
*~
.vscode/

# OS
.DS_Store
Thumbs.db

# User config overrides (keep template, ignore user-specific)
# ~/.config/system-monitor/config is user-specific and not tracked
